trigger:
- main

jobs:

- job: run

  pool:
    vmImage: 'ubuntu-latest'

  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.11'

  - script: |
      pwd
      ls -la
      python --version
      docker --version
      docker-compose --version
    displayName: 'Run'

